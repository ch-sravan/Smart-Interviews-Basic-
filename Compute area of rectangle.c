#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    long long len,bre,area;
    scanf("%lld%lld",&len,&bre);
    area=len*bre;
    printf("%lld",area);
    return 0;
}
